# macOS metadata
.DS_Store

# Codex local settings (per-developer overrides)
.codex.local.json

# Codex Unity session state — runtime-only, regenerated per session
.codex-unity/state/*
!.codex-unity/state/.gitkeep
.codex-unity/logs/
